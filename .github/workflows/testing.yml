name: Backend tests & checks

on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]

defaults:
  run:
    working-directory: backend

jobs:
  test:
    name: Backend tests & checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run all app tests
        env:
          CI: '1'
          SECRET_KEY: 'ci-secret-key-for-tests'
        run: |
          python manage.py test apps.users apps.payments apps.courses apps.carts --no-input -v 2

      - name: Lint (flake8)
        run: |
          pip install flake8
          flake8 apps/ --max-line-length=120 --extend-ignore=E501,W503
