// src/schemas/types.ts

export interface Track {
  id: string;
  title: string;
  price: number;
  image: string;
  bgColor: string;
  accentColor: string;
  titleColor: string;
  subtitleColor: string;
  arrowColor: string;
  arrowBgColor: string;
  moreColor: string;
}